# Authentication Configuration

# Required: Password for accessing protected pages
# Use a strong password with at least 8 characters, including uppercase, lowercase, numbers, and symbols
PAGE_ACCESS_PASSWORD=your-strong-password-here

# Optional: Session duration in hours (default: 24)
# How long users stay logged in before needing to re-authenticate
SESSION_DURATION_HOURS=24

# EmailJS Configuration (for portfolio requests)
# Get these from https://www.emailjs.com/
NEXT_PUBLIC_EMAILJS_SERVICE_ID=your_service_id
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=your_template_id
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=your_public_key